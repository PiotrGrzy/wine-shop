import styled from "styled-components"
import StyledLink from "styles/StyledLink"

export const CountryLink = styled(StyledLink)`
  font-size: 1.8rem;
  padding: 0.5rem;
  text-align: center;
  color: var(--secondary-light);
`
