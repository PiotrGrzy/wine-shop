import styled from "styled-components"

const BgBackDropWrapper = styled.div`
  margin: 2rem 0;
  display: flex;
  justify-content: center;
  background-color: var(--bg-backdrop-white);
`
export default BgBackDropWrapper
